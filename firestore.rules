
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules to allow all read and write operations, 
    // effectively disabling security restrictions as requested.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
